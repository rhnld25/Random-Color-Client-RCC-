# Random Color Client (RCC)

A small web experiment exploring:
- session-based determinism
- randomness vs stability
- client-side session identity
- simple UI truthfulness (color + outline)

Built with:
- HTML
- CSS
- Vanilla JavaScript

Notes:
- Color is stable per tab session using sessionStorage
- Session ID is hashed to select a deterministic theme